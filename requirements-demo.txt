# Extra dependencies for the DAWN speed-race demo (app.py).
# Install on top of requirements.txt (torch / transformers / numpy already there).
#
#   pip install -r requirements.txt
#   pip install -r requirements-demo.txt

gradio>=4.0,<5.0

# pulled in transitively by llada/gdllm_utils.py (imported via llada/generate.py)
matplotlib
seaborn
